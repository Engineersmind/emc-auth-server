# Binaries
emc-auth-server
emc-auth-server~
*.exe
*.exe~

# Logs
*.log
server*.log

# Environment
.env
*.env.local

# Certs
certs/*.key
certs/*.pem

# Build
dist/
build/

# IDE
.vscode/settings.json
.idea/

# OS
.DS_Store
Thumbs.db

# Go
vendor/

# Node (admin UI)
ui/node_modules/
ui/dist/
ui/.env

# Go embed.FS — built by npm run build (or Docker Stage 1)
internal/ui/dist/

# Coverage / test artifacts
coverage
coverage.out
*.out

# Claude settings & our local planning docs — keep private, never push.
# (Existing tracked docs like README.md, REVIEW.md, docs/ stay in remote.)
.claude/
CLAUDE.md
PROJECT_PLAN_SUMMARY.md
GAP_ANALYSIS.md
ROADMAP.md
PRODUCTION_READINESS.md
MEMORY*.md
FRONTEND_M2M_PLAN.md
FRONTEND_RBAC_NEXT_STEP.md
docs/SPRINT_*.md
EMC_M2M_Postman_Collection.json

# Postman collection, environment and its verifier — local tooling, not shipped.
# The whole directory, which supersedes master's narrower
# postman/verify-collection.ps1 entry.
postman/

# Local verification / demo scratch — dated run-throughs and throwaway drivers.
# Named individually rather than as docs/*: docs/ holds real deliverables
# (DEPLOYMENT.md, swagger.json) that must stay in the remote.
docs/*_VERIFICATION_*.md
scripts/demo-*.sh

# WebAuthn/passkey working notes — superseded local scratch, per the status
# banner in WEBAUTHN_MASTER_PLAN.md §12. Named individually rather than as
# docs/WEBAUTHN_* because two of that family ARE deliverables and must reach the
# remote: PASSKEY_API_CONTRACT.md (referenced by EMC-Auth-Frontend#134) and
# WEBAUTHN_MASTER_PLAN.md (the single source of truth for the track).
docs/WEBAUTHN_API_CONTRACT_ACTUAL.md
docs/WEBAUTHN_DEMO_COMMANDS.md
docs/WEBAUTHN_DEMO_RUNBOOK.md
docs/WEBAUTHN_FRONTEND_PROMPT.md
docs/WEBAUTHN_PREREQUISITES.md
docs/WEBAUTHN_TEST_PROCEDURE.md

# Passkey gap tracker — local follow-up notes, same class as GAP_ANALYSIS.md.
# Its contents become GitHub issues; the file itself stays private.
docs/PASSKEY_GAPS.md
