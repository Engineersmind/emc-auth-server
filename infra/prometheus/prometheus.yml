global:
  scrape_interval:     15s
  evaluation_interval: 15s

rule_files:
  - "audit_alerts.yml"

scrape_configs:
  - job_name: "emc-auth-server"
    static_configs:
      - targets: ["app:9090"]
    metrics_path: /metrics
